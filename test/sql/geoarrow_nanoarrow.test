
require spatial
require nanoarrow

# Check that exporting Arrow data before registering extensions doesn't result
# in an WKB extension type

# Check that importing Arrow data with GeoArrow extension types doesn't result
# in a geometry column


query I
CALL register_geoarrow_extensions()
----
true

# Check that exporting Arrow data before registering extensions doesn't result
# in a WKB extension type

# Check that importing Arrow data with GeoArrow extension types doesn't result
# in a geometry column
